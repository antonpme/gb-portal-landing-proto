# CHANGELOG - portal-landing-prototype

2026-08-10 · first submission of the portal landing prototype (hero A/B, gift grid, sticky bar) · shown
2026-08-11 · variant A accepted as the primary hero; sidebar rebuilt pixel-perfect from Figma nodes 3550-26803 / 3561-3633 · approved (hero A) / shown (sidebar)
2026-08-12 · sidebar nav switcher: nav=live (default, untouched) / nav=russell (7 renames and moves from Russell's Slack feedback), toggle in the demo pill + ?nav= URL param · draft
2026-08-12 · nav=ton third variant (masthead with the canonical GildedBox logo + Explore Gifts, Company section with Upload Logo and Company Management, Russell naming kept) + catalog overlay with placeholder cards · draft
2026-08-12 · ton final layout (My Teams back to Admin top as a personal item, My Company absorbed by Company Management); nav=russell redefined per Ton as ton minus masthead (literal Russell build preserved in git history); company name shown as GildedBox · draft
2026-08-12 · variants polish wave: Upload Logo demo-upload (box swaps to the GildedBox mark, Reset restores), overlay close moves to the panel's front-left edge, masthead Explore Gifts goes black filled, catalog panel widens to 95% (Tiffany inset), item order = frequency + gift lifecycle with settings sinking (Gift Recipients and Account Information join Account) · draft
2026-08-12 · page metablock in the has-gifts state (breadcrumbs Home > My Gifts with the Customer crumb deliberately dropped + serif title card with gift glyph and GET HELP), title follows the nav variant · draft
2026-08-12 · the real v1 AI Concierge catalog lands in the overlay: vendored build in catalog/ framed as ?embed=1 (headerless additive mode in v1), panel title dropped in favor of the catalog's own serif hero; pill label Ton renamed to Anton (internal nav=ton values kept) · draft
2026-08-12 · fork correction per Ton: russell = fork snapshot (live order + renames, BA before CM, short name, no upload demo, no metablock), everything post-fork is ton-only; metablock gated to ton · draft
2026-08-12 · the real site header returns to nav=live and nav=russell (v1 category page header, pixel transfer, shared 1620px wrapper; ton keeps the masthead instead); the only sanctioned break of the live freeze · draft
2026-08-12 · the live footer lands on the landing itself (v1/live footer, pixel transfer, all three variants, in flow; bar-height pad in the gifts state so the bar never buries the legal row) · draft
2026-08-12 · overlay polish: embed hides v1's footer and sticky Book a Meeting; close control re-cut to the v1 drawer geometry (h 80 head, 32px gutter, 44px circle, 22px glyph) · draft
2026-08-12 · empty state centers optically between header and viewport bottom (44/56 spacers, safe fallback to flow); small screens: halves stack, OR goes horizontal, clamp type roles · draft
2026-08-12 · live-portal reality pass: sticky elevated header with the portal composition (Gifts / Customize+Beta / Portal / Explore, black Book a Meeting, My Portal, cart with count, search), Zinc 100 page ground with white cards, portal footer line by line (crumb strip, italic address, exact columns, legal row), wrapper widened to the v1 formula, hero kept as a 1200px title composition · draft
2026-08-12 · demo pill redesigned into the PROTO panel: vertical dark (Zinc 950) service chrome fixed to the right screen edge, mono caps labels, grouped HERO / NAV / GIFTS controls, collapsible edge tab; deliberately outside the design language after Valerie read the pill as design and it lay over the metablock (Ton's feedback) · draft
2026-08-12 · portal precision wave: full token layer in :root (zinc 50..950, blue 600/700, off-scale live values fixed as tokens, zero hex left in rules), header to the live portal metric (80px bar, 16px items, portal button theme 400/16/-0.4 on BOOK A MEETING and MY PORTAL, constant soft shadow, live gold #bea042), metapanel and crumbs to the measured numbers (24 pad, radius 4, serif 32 w300 zinc-600, GET HELP zinc-900 outlined 40px, 14px crumbs with SVG chevron), gift cards to the live card (zinc-50 fill, shadow-md, zinc-200 badge, centered 20px name + price line, 48px zinc-800 button), footer to the public measurement (pure black, serif 22 titles, Inter 300 14 links, computed paddings, #3d3d3d hairlines; portal crumb strip kept); SPEC.md with the verified computed table · draft
2026-08-12 · diff-fix wave (our Pages vs live portal, one 2258 viewport): wrapper becomes the live .container ladder (100% + 24px pads, 1600 cap from 1600, 1920 cap + 48px pads from 1920, 16px pads under 768; footer joins the same ladder, v1 1600 formula lifted), header buttons and nav take the live Tailwind steps (buttons 14/36/16 -> 16/42/20 at 1280 -> 48 at 1536, nav items 14 -> 16 at 1280), BOOK A MEETING loses the calendar glyph and the filled border (live is text-only, 172px vs our old 198), MY PORTAL keeps its spec-confirmed 1px zinc-900 border · draft
2026-08-12 · all buttons leave the Figma Oro scale for the LIVE portal button theme (Ton, no new measures: Inter 400 UPPERCASE ls -0.4px, labels 14 -> 16 at 1280, min-h ladder 36/42/48, radius 5, no shadows): landing CTAs (were 56px/600/1.5px), masthead Explore Gifts, sticky-bar buttons; primary CTA weight now carried by width and placement; SYSTEM.md: Oro scale marked DEPRECATED, live theme = canon; PROTO panel loses the HERO A/B group (variant A is the base, ?v=b stays working in code); hero eyebrow 14 -> 16px (Ton's micro-edit) · draft
2026-08-12 · metablock in nav=ton becomes an editorial band (Ren's proposal, accepted by Ton): no card, border, shadow or gift glyph, crumbs straight on the zinc-100 ground, 8px to a 40px Noto Serif 300 zinc-900 title with GET HELP on its baseline, 36px clean air below, ~70px vertical economy; live/russell keep the boxed panel for comparison; radius audit: everything on the 5px --radius token (sidebar blocks and boxed metapanel were 4px, upload-logo ghost 6px, all marked DEVIATION per Ton; catalog overlay gets front-corner radius; conscious exceptions: 50% circles, 999px cart pill, PROTO service panel) · draft
2026-08-12 · overlay bug wave (Ton's list): page scroll-locked behind the open catalog with v1's --scrollbar-width compensation (double scrollbar gone), overlay top bar removed, the close control moved INTO the framed catalog's nav row (v1 embed: drawers' 44px icon-button first before GOURMET, postMessage gb-close-catalog; Esc and backdrop as before), v1 embed also drops the catalog hero (Ton+Ren: EXPLORE GIFTS already named it, vertical goes to product), the dark value-props band, floating Chat with us and Back to top (on-the-spot: floats over the frame, sticky nav jumps anywhere); fresh v1 build vendored to catalog/ with the vendoring transform (bundle's absolute /assets/ to ./assets/, as the previous vendoring did, required for subpath hosting) · draft
2026-08-12 · overlay motion + drawer wave: catalog overlay gets Ren's choreography accepted by Ton (enter: backdrop 300ms at once, panel slides from off-screen 500ms cubic-bezier(.22,1,.36,1), content fades+lifts 12px 400ms with 150ms delay; exit faster: panel away 380ms ease-in with content riding, backdrop relights 300ms after 80ms; reduced-motion = fades; display:none only after the exit plays); embed cross repositioned out of the nav flow into the left margin (GOURMET back on the cards' line, gutter 48 -> 68 minimal, row 80px one height with drawer headers); embed drawers lock the catalog scroll and dim it with a standard backdrop; landing EXPLORE GIFTS (right half) opens the same overlay in nav=ton; sticky bar stops at the footer's top edge (old footer pad removed as dead) · draft
2026-08-17 · button ladder trued to the live measurement (harvest public-home 2026-08-17): the 48px min-height step moves from 1536 to the live threshold 2000 (sweep: 1999 = 42, 2000 = 48) on .gbh-cta and the shared LIVE BUTTON LADDER; header CTA gap 12 -> 20 from 1280; button letter-spacing -0.35 below 1280 (-0.4 from 1280 stays); header bar 80px at every width (the 72px mobile override lifted); CTA labels wrapped in span[data-pc-section="label"] per live's PrimeVue anatomy, structure only, label typography waits for the portal measurement; same steps synced to system\tokens.css · draft
2026-08-17 · gifting restructure in nav=ton (feedback R+T 2026-08-17): masthead EXPLORE GIFTS button removed ("too many buttons competing", the prepared fallback of DECISIONS-BANK item 3.5), masthead is the quiet wordmark alone, no card border or white ground, vertical pads tightened to 8px so no hole remains; Explore Gifts becomes a regular menu item, first in the first section above My Designed Gifts (gift.svg reused, no new icons), opening the same catalog overlay via the same openCatalog wiring; section titles in ton: Account -> Gifting, Admin -> Account, Company -> My Company; user identity (Anton Parkhomenko + email) moves from the Gifting section into the Account (ex Admin) section; Gift prefix dropped in Gifting: Redeem Pages, Order History, Design Approvals (My Designed Gifts and Gifting Campaigns untouched); Brand Assets in My Company временно выключен (фидбек T 2026-08-17, code kept); nav=live and nav=russell verified pixel-identical by pre/post DOM dump diff (titles, item sets, order, icons, top coordinates) · draft
2026-08-17 · hero-block CTAs move to BUTTON XL, the largest size class the live site carries (feedback R via T 2026-08-17 «самый большой доступный размер»): ladder = live product page Continue (harvest public-product + public-product-sweep 2026-08-17), min-h 56 / 64 from 1280 / 72 from 2000 (sweep: 1279 = 56, 1280 = 64; 1999 = 64, 2000 = 72), pad-y 8 constant, pad-x 24 -> 32 from 1280, radius stays --radius; typography deliberately untouched (label scale waits for the portal measurement); .btn-bar sticky-bar buttons and header .gbh-cta stay on the compact 36/42/48 ladder, verified by instrument (proto-cta-xl + proto-cta-xl-russell runs: header 36/42/48 on prior thresholds, sidebar untouched, landing block change shared across nav variants as expected); XL tokens added to system\tokens.css · draft
2026-08-17 · nav=ton only: Account Information moves out of the Gifting section (was last visible item) into the Account (ex Admin) section as the first item right after the identity block (Anton Parkhomenko + email), before My Teams; done in the accTon/admTon order arrays only, accLive/accRussell/admLive/admRussell untouched; nav=live and nav=russell verified DOM-identical to the pre-change state by headless dump diff (titles, item sets, order, hidden carriers) · draft
2026-08-17 · nav=ton only: validated menu order applied (validated by Ren, requested by Ton 2026-08-17): Gifting follows the gift lifecycle axis (Explore Gifts, My Designed Gifts, Design Approvals, Gifting Campaigns, Redeem Pages, Gift Recipients, Order History, Thank You Notes); Account = personal cluster then patterns (Account Information, Password, My Teams, Payments, Notifications, Log out last); hidden carriers Brand Assets / My Company ride at the array tails; accTon/admTon arrays only, nav=live and nav=russell verified byte-identical to the pre-change baseline by normalized headless DOM dump diff · draft
2026-08-17 · hero-block CTAs step down from BUTTON XL to a new BUTTON L class (Ton's requirement «на размер меньше» 2026-08-17): min-h 48 / 56 from 1280 / 64 from 2000, pad-y 8 constant, pad-x = 0.5H (24 / 28 from 1280 / 32 from 2000), same 1280 and 2000 thresholds as the live ladders; L has NO live measurement, interpolated per Ren's proportion formula, marked so in provenance; typography untouched; XL stays in the system (live off the product Continue), just unworn; verified by instrument (proto-cta-xl rerun: hero CTAs 48/56/64 with pads 24/28/32, header 36/42/48 and sticky bar untouched); L tokens added to system\tokens.css, formula block wearer list updated · draft
2026-08-17 · header CTA labels take the live label typography (live public-home label span, harvest 2026-08-17: 11px / 600 / ls 1px / line-height normal, 12px from 2000; uppercase and color inherited), applied by T's decision 2026-08-17 (screenshot comparison vs live) ahead of the portal measurement, which stays wanted as confirmation; button heights (36/42/48), paddings and gap untouched; verified by instrument (proto-live rerun with label targets: label 11/600/1 at 1280..1920, 12/600/1 at 2258, dark CTA width now 149.81 at 1280..1920 and 158.52 at 2258, the exact live numbers, was 172.44); label tokens added to system\tokens.css, TYPE-SCALE.md label role restatused to applied-by-T-decision · draft
2026-08-17 · fourth nav variant Ren added (accepted by Ton 2026-08-17): a SKIN over nav=ton, derived, not a fork snapshot (setNav('ren') keeps data-nav=ton on the body with the same order arrays, labels, masthead and Explore Gifts wiring; the body gains the ren-skin class and ALL visual rules live under body.ren-skin only, so future structural ton edits are inherited); editorial skin, five decisions: sidebar de-boxed (white cards, borders and radii gone, sections sit on the zinc-100 ground with 24px air + zinc-200 hairlines, none under the wordmark), items in Title Case (markup already carried it, the uppercase transform goes) Inter 400/14 -0.2px zinc-600 with active 500 zinc-900, section titles as 11px eyebrow caps +1.2px 500 zinc-400 (the landing's COMPANY GIFTING FOR language), active item = 2px zinc-900 left rule (inset shadow, no layout shift) with zinc-50 hover 150ms ease and no blue anywhere in the skin sidebar, quieter secondary (icons 18px at 0.45 opacity, 0.9 active; Upload Logo from gray button to a quiet 13px zinc-500 text link, underline on hover, glyph gone; identity name 14/500 zinc-900, email 13 zinc-500); PROTO panel gains a Ren button after Anton, ?nav=ren URL param works; verified headless: ren visible order/labels identical to ton, catalog overlay opens from Explore Gifts and closes by Esc, PROTO switching in and out clears the skin, nav=live / nav=russell / nav=ton computed-style sidebar dumps byte-identical to the pre-change HEAD baseline · draft
2026-08-17 · Ren skin refinement wave (feedback T 2026-08-17 evening, nav=ren only): menu labels denser (400 -> 500, active 500 -> 600, normal color zinc-600 -> zinc-700; size 14 and Title Case untouched), icons brighter (opacity 0.45 -> 0.65, active 0.9 -> 1.0; 18px kept), hover wash gains border-radius --radius (5px, verified computed), page ground zinc-100 -> zinc-50 (token --zinc-50, ren only); Start With Us | Gift on Your Own cards checked: already pure #ffffff via var(--white) on .hero-block in ALL variants, no edit needed; nav=ton / live / russell verified unchanged by computed dump (body zinc-100, labels 400, icons 1.0 at 24px, hover radius 0) · draft
2026-08-17 · ren skin hover wash lifted to zinc-100: the zinc-50 wash became invisible once the ren body itself went zinc-50 (caught in the refinement wave's own report) · draft
2026-08-17 · Ren skin night wave (feedback T 2026-08-17 night, nav=ren only): masthead logo leaves the center for the shared 16px left axis the eyebrows and menu items ride (computed: logo left 40 = eyebrow left 40 = item content edge 40; the label glyphs themselves start at 80 behind the 24px icon column), vertical pads stay 8px, no growth; zinc-200 hairlines between sections removed entirely, sections separate by air alone at 40px (masthead keeps its tighter 24px to the first section), the eyebrow is the only marker of a section start; active item brightens without weight play (owner vetoed the heavier cut «некрасиво, когда активное меню становится жирнее»): active label back to 500 = normal weight, color zinc-950, active icon reads near-black via filter brightness(0) saturate(100%); hover-on-active bug fixed: the hover radius rounded the inset 2px left rule, the active item now opts out of the radius so the rule stays straight (computed on hover: radius 0, shadow intact; normal items keep the 5px wash) · nav=ton verified unchanged by computed dump (logo centered at 100, hairlines 1px solid, sidebar gap 16, active label 400, no icon filter, blue active plaque, body zinc-100) · draft
2026-08-18 · Ren skin: the active menu item returns to the system blue accent (feedback T 2026-08-18 night, nav=ren only): the owner's logic is blue = the state accent family (active radios, switches, checkboxes, CTAs), black = permanent furniture, and the active item is a state; the 2px left rule, the active label and the active icon all take var(--blue-600), weight stays 500 (the no-weight-play veto holds), the straight-rule hover opt-out stays; the icon recolors by mask re-render (active img hidden, the .snav-icon box paints an 18px ::after masked by the same SVG file via --snav-icon-url, copied from each img src by a one-pass script), superseding the near-black brightness(0) filter; verified computed on :8140 (active label rgb(37,99,235) at 500, rule inset 2px rgb(37,99,235), ::after 18x18 blue-600 masked by gift.svg, inactive zinc-700/500 with 0.65 icons untouched) and nav=ton unchanged (blue-50 plaque, label 400 zinc-950 scale, img block, no mask) · draft
2026-08-18 · Start Gifting block, opt-in via ?hero=start (marker gbppl-hero-start, feedback R 2026-08-18 call + T's decisions): the landing leaves the two-equal-doors composition for ONE route, Explore survives but submits; the block replaces only the two-CTA zone (the greeting above stays, so it composes with ?v=a|b and every ?nav=), body attributes are set ONLY when the parameter is present so the approved default view keeps its exact DOM; composition is blockless (no cards, borders, dividers or vertical OR), a 2x2 of pure typography with no icons ("too many icons", R), no rules and no colored markers (no gold per BRAND, blue reserved for state per Ton-5), the four lines carried verbatim from Russell (Recommendations / Brand-first design / Portal automation / No minimums); block title = the live section-h2 role Noto Serif 300 32 -> 36 from 1280, lh 1.4 (harvest public-our-story + public-platform 2026-08-17), a level under the live 48/400 page h1; cell type steps up one notch over the two-half block (title 22, line 20 vs 19) with 80/96 column and 48/56 row air; ?grid=weight (default, Inter 600 title over Inter 300 line) | serif (Noto Serif title, same 300); actions = filled Blue 600 START (primary) + OUTLINED black EXPLORE GIFTS (furniture, opens the same catalog overlay in ton/ren), both on BUTTON L of the proportion formula (48/56/64, pad-y 8, pad-x 24/28/32, radius 5, Inter 400 uppercase 14 -> 16) plus a felt min-width floor 176/200/220 for the one-word START label; 2x2 folds to one column on phones with the buttons stacked full width · draft
2026-08-18 · Ren skin, feedback R 2026-08-18 ("панель понравилась, шрифт чуть крупнее и виднее, панель чуть уже"), ren only: menu labels 14 -> 15px and zinc-700 -> zinc-800 with the weight held at 500 (the no-weight-play veto and the blue active state untouched), line-height kept at the inherited 21px so the 46px item geometry does not move; sidebar rail 320 -> 280px, undone at <=900 where the rail goes full width; widest ren label Gift Recipients (Addresses) measures 190px of the 208px label column, one line with 18px of slack (verified by instrument at 1280 and 2258); nav=live / nav=russell / nav=ton verified byte-identical to the pre-change baseline by computed-style DOM dump at 1280 and 2258, ?hero=start absent from all of them · draft
2026-08-18 · Start Gifting, second pass for the live show (marker gbppl-hero-start-2, feedback T 2026-08-18, ?hero=start only, ren skin only for the rail): ONE COMPOSITION instead of two heads, the greeting is NOT removed (Russell is attached to it) but demoted to context under the attribute only — eyebrow COMPANY GIFTING FOR: to the quiet 11px caps cut (500, ls 0.12em, zinc-500), pipe list 28/500/zinc-900 -> 22/400/zinc-700 with zinc-200 hairline separators at 14px each side, air below it 80 -> 40, and variant B's h1 + sentence demoted by the same rule (22/400 zinc-700 + 17 zinc-500) so ?v=b grows no second title; the resulting ladder descends 36 serif (Start Gifting, the page dominant) > 22 context list > 20 cell titles > 18 cell lines, which cost the grid one notch (title 22 -> 20 both cuts, line 20 -> 18) so the cells stop standing level with the audiences line; No minimums line rewritten to Russell's fuller promise "No minimums, no commitments. Send a single gift or a hundred, exactly when you need." and verified two lines like its neighbours at 1280 and 2258 (the 18px step is what buys the second line at 1280, 20px wrapped to three); START / EXPLORE GIFTS min-width floor widened ~24%, 176/200/220 -> 220/248/272 (at 1280 the pair plus its 24 gap = 520 of the 960 grid); the pure-typography rule is REVERSED on the owner's ask — one 24px glyph per cell from the sidebar's own site/icons set at 0.6 opacity, over the title (archery-target = targeted recommendations, brand-assets = the product's own brand mark, cogwheel = automation, gift = a single gift or a hundred), no new art drawn, plus an internal hairline CROSS in zinc-200 (one vertical at the middle of the column gap, one horizontal at the middle of the row gap, drawn as ::before/::after on the grid box with grid-auto-rows 1fr so the horizontal lands exactly on the seam) with NO outer edges, dismissed entirely at <=900 where the grid is one column · verified by instrument: all four cells two lines at 1280 and 2258, buttons 248/272 equal width, ren rail items still 46px tall · draft
2026-08-18 · Ren skin: menu labels 15 -> 16px (feedback T 2026-08-18), weight held at 500 and color zinc-800 untouched (the no-weight-play veto stands), line-height restated explicitly at 21px so no item moves — computed diff vs the pre-change baseline shows only fontSize, every label rect identical to the hundredth of a pixel and every item still 46px; widest label Gift Recipients (Addresses) now measures 203.08px of the 208px label column at both 1280 and 2258, one line with ~5px of slack (was 190 at 15px: the rail is now at its ceiling, a longer label would wrap); nav=live / nav=russell / nav=ton and the whole default view verified byte-identical to the pre-change baseline by full-page PNG hash at 1280 and 2258 · draft
2026-08-18 · Start Gifting, third pass (marker gbppl-hero-start-3, feedback T 2026-08-18 on the screenshot, ?hero=start only): (1) VERTICAL RHYTHM corrected — the block used to leave the same air above Start Gifting as inside it (40 up against 56 down), so the eye grouped the context line with the title and read the grid and the buttons as two further decks; the gap down from the audiences line is now twice the gap inside the block (64 over 32 below 1280, 72 over 36 from it, 48 over 28 at <=900; grid-to-buttons 40/44/32), which makes title + grid + buttons one cluster with the context line held apart above it; (2) the context line takes half a step back up after going inaudible at 11/22 — eyebrow 11 -> 12, pipe list 22 -> 24 (the default hero's own .sub size, zinc-700 and 400 unchanged), ladder now 36 serif > 24 context > 20 cell titles > 18 cell lines and Start Gifting still dominates by family, size and colour; (3) TWO ALTERNATIVE ARRANGEMENTS of the same block behind &layout=, one DOM for all three so a wording fix can never drift — &layout=grid is the approved 2x2 with the hairline cross and is what renders with no parameter or an unknown one, &layout=split is editorial asymmetry (5fr/7fr of 1120 with an 80px gutter = 433 left / 607 right at 2258, 333/467 inside the 880 content column at 1280; left column carries the title one step up at 36 -> 44 from 1280 with line-height 1.15 so its cap line meets the first point's across the gutter, one supporting line "One route in, from the first recommendation to the last delivery." at the 18px cell-line role, and the two buttons stacked vertically because side by side they need 2x248+24 = 520 against a 433 track; right column is the four points in one column with zinc-200 hairlines BETWEEN the rows only and the 24px glyph moved beside the title spanning both its rows; a fourth 1fr grid row absorbs the tall right column's slack so the head stays a tight group), &layout=band is one horizontal ribbon of four columns with the glyph over the title and vertical zinc-200 hairlines between and none at the ends, the figure the live home page draws under its benefits strip (grid widened 960 -> 1120, gaps zeroed and the air carried as 24px cell padding so the hairline sits at the true middle, tracks minmax(0,1fr) so a long word cannot push them off the beat, cell line 18 -> 16 the one size concession, columns measure 280 at 2258 and 220 at 1280); both fold at <=900 into the same single column the 2x2 already folds into, split reordering on the way down to head, lede, buttons, points; &grid=weight|serif composes with all three (it cuts .hs-cell-title, which no layout redefines) · verified by instrument: rhythm 72/36/44 at 1280 and 2258 in every layout, band columns equal to the pixel, split buttons 248/272 equal width, and the default view (no parameters) plus ?v=b / ?nav=ton / ?nav=ren byte-identical to the pre-change HEAD baseline by computed-style dump at 1280 and 2258 · draft
2026-08-18 · band becomes the default arrangement of Start Gifting and START learns where it leads (marker gbppl-start-panel, Russell 2026-08-18: band chosen out of the three, and «design what happens when we click Start»): (1) ?hero=start with no &layout= (or an unknown one) now renders BAND, the horizontal ribbon of four columns the live home page draws under its benefits strip; grid (the earlier default 2x2 with the hairline cross) and split stay one parameter and one PROTO button away, the Block row is unrenamed and only its lit button moves; (2) START opens a new right slide-over panel (id gbStartPanel) in every layout and every nav variant, built on a headless measurement of the live site taken the same day — www.gildedbox.com's own header START GIFTING opens a 600px full-height drawer, not a centered modal: backdrop rgba(0,0,0,.6), header band 78px on Zinc 100 with a 42px radius-5 close, body on Zinc 50, content column 472 = 600 - 2x64, underline fields (border-bottom 1px Zinc 400, h 56, Inter 300 16), a full-width Blue 600 CTA at h 56 radius 5, body overflow hidden and Esc closing; their BOOK A MEETING is a link to /page/contact-us-219, a page of the same underline fields, and a Pubble chat widget (370x600) is the third channel, so the three doors are the three ways the live business already reaches a human; (3) what we did NOT take: the live's sign-in wall (their START asks for an account before it asks what you need, ours answers with the route and leaves the account for later) and the serif title inside the header band (our band carries controls only); (4) execution is the catalog overlay's own language: same choreography verbatim (backdrop 300ms, panel 500ms cubic-bezier(.22,1,.36,1), content +12px 400ms after 150ms, exit 380ms ease-in with the backdrop trailing 80ms, reduced motion = fades), same rgba(24,24,27,.4) dim, same front-corner radius and Zinc 50 ground, same --scrollbar-width lock under its own body class so the two layers never uncover each other, cross in the header FIELD and never on the content, at the left as DECISIONS-BANK item 4 fixed it, with the step's BACK beside it in the same band; (5) content, three doors as ONE white form with internal Zinc 200 hairlines (Russell's whole-forms principle): Live chat / Talk to us now, Book a meeting / Pick a time that suits you, Request a call / Leave your number, we call you back, each a full-width button with a 24px glyph cut inline in the very recipe the site/icons set was exported in (stroke Zinc 500, width 0.8, square caps: nothing in the set says chat, calendar or handset, so they are cut to match rather than borrowed from a second family); (6) every door swaps the view INSIDE the panel and BACK returns without resetting what the guest typed — chat = a stand-in window (one expert message in a Zinc 100 bubble, underline entry, SEND disabled with the prototype saying so out loud), meeting = a stand-in August 2026 calendar Sunday-first with past days and weekends inert, slot chips at 32px, selection Blue 600 filled on both day and chip (Ton-5: blue is the accent of state) and CONFIRM MEETING disabled until both are picked, then the calendar gives way to one quiet line; call = three underline fields (Name, Phone, Best time to call) with 11px caps 600 labels and a Blue 600 focus rule, submitting to the quiet «We got you. Expect a call within one business day.» with a blue check, no banner and no green; Esc and the backdrop close, the dialog takes focus on open and hands it back to the START that opened it, the drawer goes full width under 700 · verified headless at 1280 and 2258: band default with the Block row lit, panel 600x100% pinned right with a radius 5 front edge and an 80px band, all three doors there and back, confirm gating and both confirmations, underline computed at border-bottom 1px rgb(161,161,170) / 16px / 300, Esc and backdrop close with focus returned, Explore Gifts and the catalog overlay untouched, and the whole default view (no parameters) plus ?v=b / ?nav=ton / ?nav=ren byte-identical to the pre-change HEAD by full-page PNG hash · draft
2026-08-18 · four hand-cut glyphs give way to the real Nucleo (marker gbppl-nucleo-glyphs, ?hero=start only): the full 3196-icon Nucleo set was exported to assets/nucleo-full, so the placeholders drawn while only a 20-file subset existed are retired for their genuine articles, each retuned into the set's own recipe rather than dropped in raw — the sources are 48-grid at stroke-width 1, ours is a ~22.8 box at 0.8, so every file keeps viewBox 0 0 48 48 with the intrinsic size declared at 22.8 and the weight restated as 1.684, which is the same fraction of the box (0.0351) the shipped icons/ carry, plus stroke #71717A, square caps, miter joins and the per-path butt caps Nucleo marks where a line must not overhang its frame; (1) the Brand-first design cell drops icons/brand-assets.svg — a picture frame drawn by hand in 2026-08-12 as a same-language approximation, and read as "image", not "brand" — for icons/color-palette.svg, Nucleo's swatch fan, which says brand colour specification in the one glyph; its fan blade ships from Nucleo as a filled stroke-outline (1 unit wide against our 1.684) and would have read visibly thinner than the card it fans out of, so the outline is stroked at 0.684 over its own fill, which grows it by 0.342 on each side to exactly family weight, and Nucleo's degenerate zero-area subpath at the tip is dropped so the added stroke has nothing to spike on; (2) the three Start-panel doors leave their inline hand cuts for icons/chat.svg (48-messages, two overlapping bubbles), icons/calendar.svg (48-calendar-check, chosen over 48-calendar-event whose inner grid closes into a hash at 24px, and semantically the booked date rather than a bare month) and icons/phone.svg (48-phone-volume, the two arcs are what say "we ring you back" where the bare handset says "here is a phone"); the calendar's three day dots ship as plain fills and would have gone sub-pixel, so they carry the family stroke on top; (3) the doors also change how they load — inline <svg stroke="currentColor"> to <img>, the way every other glyph on the page is fetched, which makes all four files live assets and trades the browser's default black buttontext for the set's Zinc 500, the colour the rest of the page's glyphs speak (the cells hold their own 0.6 opacity on top) · verified headless: the four cell glyphs sit at one weight beside archery-target / cogwheel / gift at 1:1 and at 7x, all three doors open and come back, and the default view plus ?v=b / ?nav=ton / ?nav=ren are byte-identical to the pre-change HEAD by full-page PNG hash at 1280 and 2258 · draft
